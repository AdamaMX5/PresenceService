# PresenceService
A Fast Websocket Service, to share positions of users. Multi Server able because RedisDB with Subscripe&amp;Publish.

## start
```bash
docker compose up

## security check for JWT
add /secrets/auth_public_key.pem
